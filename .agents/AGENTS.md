# Custom Project Rules

- **Git Commits**: After completing any task, file edits, or feature implementation, always stage and commit the changes to the git repository. Do not run `git push`—the user prefers to review and push the changes manually.
